# AKS override for portal-oauth2-proxy-config. Domain switches to
# platform.eu-sovereigndataplatform.com (port 443, no :8443 suffix).
# The in-cluster redeem/profile/validate/jwks URLs stay the same because
# they hit the Keycloak Service directly in-cluster.
apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-oauth2-proxy-config
  namespace: uwv-platform
data:
  oauth2-proxy.cfg: |
    provider                = "keycloak-oidc"
    client_id               = "portal"
    oidc_issuer_url         = "https://keycloak.eu-sovereigndataplatform.com/realms/uwv"

    skip_oidc_discovery     = true
    login_url               = "https://keycloak.eu-sovereigndataplatform.com/realms/uwv/protocol/openid-connect/auth"
    redeem_url              = "http://keycloak.uwv-auth.svc.cluster.local/realms/uwv/protocol/openid-connect/token"
    profile_url             = "http://keycloak.uwv-auth.svc.cluster.local/realms/uwv/protocol/openid-connect/userinfo"
    validate_url            = "http://keycloak.uwv-auth.svc.cluster.local/realms/uwv/protocol/openid-connect/userinfo"
    oidc_jwks_url           = "http://keycloak.uwv-auth.svc.cluster.local/realms/uwv/protocol/openid-connect/certs"

    cookie_name             = "_uwv_portal"
    cookie_secure           = true
    cookie_httponly         = true
    cookie_samesite         = "lax"
    cookie_expire           = "8h"
    cookie_refresh          = "30m"
    cookie_domains          = ["platform.eu-sovereigndataplatform.com"]
    whitelist_domains       = [".eu-sovereigndataplatform.com"]

    http_address            = "0.0.0.0:4180"
    upstreams               = ["http://127.0.0.1:8080"]
    redirect_url            = "https://platform.eu-sovereigndataplatform.com/oauth2/callback"

    pass_user_headers       = true
    pass_access_token       = true
    set_xauthrequest        = true
    set_authorization_header = false

    email_domains           = ["*"]
    skip_provider_button    = true

    scope                   = "openid roles"
    insecure_oidc_allow_unverified_email = true

    oidc_groups_claim       = "roles"

    skip_auth_routes        = ["^/api/health$"]

    standard_logging        = true
    request_logging         = true
    auth_logging            = true
    silence_ping_logging    = true
