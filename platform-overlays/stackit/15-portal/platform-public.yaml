# Public-facing TLS + Ingress for the platform-landing Deployment.
# Moved from infrastructure/stackit/public-ingresses.yaml — keeps the AKS
# portal's full shape in one place (alongside the Deployment that backs it).
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: platform-public-tls
  namespace: uwv-platform
spec:
  secretName: platform-public-tls
  issuerRef: { name: letsencrypt-prod, kind: ClusterIssuer }
  commonName: platform.freshstackable.com
  dnsNames:
    - platform.freshstackable.com
    - freshstackable.com
    - www.freshstackable.com
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-public
  namespace: uwv-platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - platform.freshstackable.com
        - freshstackable.com
        - www.freshstackable.com
      secretName: platform-public-tls
  rules:
    - host: platform.freshstackable.com
      http: { paths: [{ path: /, pathType: Prefix, backend: { service: { name: platform-landing, port: { number: 80 } } } }] }
    - host: freshstackable.com
      http: { paths: [{ path: /, pathType: Prefix, backend: { service: { name: platform-landing, port: { number: 80 } } } }] }
    - host: www.freshstackable.com
      http: { paths: [{ path: /, pathType: Prefix, backend: { service: { name: platform-landing, port: { number: 80 } } } }] }
