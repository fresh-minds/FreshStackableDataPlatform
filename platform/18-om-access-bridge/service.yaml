apiVersion: v1
kind: Service
metadata:
  name: om-access-bridge
  namespace: uwv-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: om-access-bridge
  ports:
    - name: http
      port: 80
      targetPort: 8002
