apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  # resourcequota-limitrange.yaml uitgeschakeld voor lokale k3d/MacBook:
  # de hard caps + ontbrekende LimitRanges voor uwv-monitoring/uwv-auth
  # blokkeerden helm-charts (Prometheus admission jobs zonder requests/limits).
  # Productie-overlay zet ze terug aan met ruimere caps.
