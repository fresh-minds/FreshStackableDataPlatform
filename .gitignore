# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage
htmlcov/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
dbt/profiles.yml

# Helm
charts/*.tgz
infrastructure/helm/**/charts/

# Kubernetes / kustomize build outputs
**/build/
*.kustomize.out.yaml

# Local secrets (never commit)
*.secret.yaml
*-secret.yaml
secrets/local/
.env
.env.local
*.pem
*.key
!opa-policies-src/data/*.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code / CCD runtime state (worktrees, scheduler locks)
.claude/

# Logs and tmp
*.log
tmp/
.cache/

# Generated synthetic data (regenerate via Makefile)
data-generation/output/

# Generated Trino catalog manifests (rendered uit *.yaml.tmpl)
platform/09-trino/catalogs/rendered/

# Synced spark-jobs (gekopieerd vanuit spark-jobs/ door deploy-platform.sh)
platform/08-spark/scripts/

# Terraform (Azure / AKS)
infrastructure/azure/terraform/.terraform/
infrastructure/azure/terraform/terraform.tfstate
infrastructure/azure/terraform/terraform.tfstate.backup
infrastructure/azure/terraform/terraform.tfvars
infrastructure/azure/terraform/*.tfplan
infrastructure/azure/terraform/crash.log

# Azure SP credentials
scripts/azure/env.sh

# VPN client certs + private keys (terraform-generated)
infrastructure/azure/vpn-client/
