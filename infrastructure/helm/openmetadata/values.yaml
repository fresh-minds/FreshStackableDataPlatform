# OpenMetadata — catalog / governance / lineage / DQ.
#
# Wordt in fase 8 geïnstalleerd. Hier alleen de values-template.
# Hergebruikt de gedeelde Postgres (uwv-data) en deelt het OpenSearch-
# cluster met Vector (uwv-meta).
#
# Chart: open-metadata/openmetadata (chart 1.5+)
#
# OIDC / SSO koppeling
# --------------------
# OpenMetadata 1.3+ verwacht de Pac4j OIDC-flow met `clientType: confidential`
# en het complete `oidcConfiguration` block. De client `openmetadata` staat
# in realm-uwv.json (clientId/secret + redirectUris); het secret wordt hier
# uit Kubernetes-secret openmetadata-oidc-client gelezen.
#
# Browser-redirects gaan naar de externe URL (https://keycloak.uwv-platform.local:8443),
# server-side token-exchange ook — daarom mountt de pod het uwv-ca-bundle
# (zelf-signed CA van ingress-nginx) in zijn Java truststore via een
# initContainer (zie `extraInitContainers` onderaan).

# Chart 1.5: alle config zit onder `openmetadata.config.*`, niet `global.*`.
openmetadata:
  config:
    # Chart-default `host: openmetadata` DNS-resolveert naar de Service
    # ClusterIP — een pod kan daar niet aan binden (alleen aan z'n eigen
    # IP of 0.0.0.0). Zonder deze override crasht de container met
    # `BindException: Address not available`.
    openmetadata:
      host: 0.0.0.0

    upgradeMigrationConfigs:
      debug: false

    # Externe Postgres (gedeeld in uwv-data)
    database:
      enabled: true
      host: postgres-postgresql.uwv-data.svc.cluster.local
      port: 5432
      driverClass: org.postgresql.Driver
      dbScheme: postgresql
      databaseName: openmetadata
      auth:
        username: uwvplatform
        password:
          secretRef: mysql-secrets               # chart-template hardcoded naam
          secretKey: openmetadata-mysql-password # idem

    # Externe OpenSearch (gedeeld met Vector)
    elasticsearch:
      enabled: true
      host: opensearch-uwv.uwv-meta.svc.cluster.local
      port: 9200
      scheme: http
      searchType: opensearch

    # OIDC via Keycloak — confidential client met server-side token-exchange.
    # Veld-namen volgen het chart-1.5 schema (schema is strict; oidcType/
    # clientId/clientSecret onder oidcConfiguration mogen niet anders heten).
    authentication:
      clientType: confidential
      provider: custom-oidc
      # Authority / callbackUrl / publicKeys / discoveryUri zijn mode-specifiek
      # — zie values-{k3d,aks}.yaml. Op k3d wijzen ze naar
      # keycloak.uwv-platform.local:8443 (via CoreDNS hosts-override); op AKS
      # wijzen ze naar de publieke keycloak.${PLATFORM_DOMAIN} (Let's Encrypt).
      clientId: openmetadata
      responseType: code
      jwtPrincipalClaims:
        - preferred_username
        - email
        - sub
      enableSelfSignup: false

      # Pac4j-config voor confidential client.
      oidcConfiguration:
        enabled: true
        oidcType: Keycloak
        clientId:
          secretRef: openmetadata-oidc-client
          secretKey: clientId
        clientSecret:
          secretRef: openmetadata-oidc-client
          secretKey: clientSecret
        # discoveryUri / callbackUrl / serverUrl zijn mode-specifiek.
        scope: "openid email profile"
        useNonce: true
        preferredJwsAlgorithm: RS256
        responseType: code
        disablePkce: false
        clientAuthenticationMethod: client_secret_post
        tenant: ""
        maxClockSkew: ""
        tokenValidity: "3600"
        # OM 1.12 default `customParams: '{}'` werkt out-of-the-box; in 1.5
        # was de chart-default `""` (lege string) wat Jackson niet naar
        # LinkedHashMap kon coercen → daar moest een post-deploy patch op
        # OIDC_CUSTOM_PARAMS. Niet meer nodig vanaf 1.12.
        customParams: "{}"
        # 1.12-only velden (genegeerd door 1.5):
        promptType: "login"
        maxAge: "0"
        sessionExpiry: "604800"

    authorizer:
      className: org.openmetadata.service.security.DefaultAuthorizer
      containerRequestFilter: org.openmetadata.service.security.JwtFilter
      initialAdmins:
        - data.steward
        - platform.admin
      # principalDomain is mode-specifiek (zie values-{k3d,aks}.yaml).
      enforcePrincipalDomain: false
      enableSecureSocketConnection: false

    # Airflow-pipelinesserver — geen Airflow geconfigureerd; uit
    pipelineServiceClientConfig:
      enabled: false

# Java truststore-uitbreiding: voeg uwv-platform CA toe zodat OM-pod TLS
# van ingress-nginx (waarachter Keycloak draait) kan verifieren bij de
# server-side token-exchange. Truststore wordt in een emptyDir geschreven
# door een initContainer en daarna door de OM-container ingelezen via
# JAVA_OPTS_APPEND (key/store-pass = changeit; alleen dev).
extraInitContainers:
  - name: import-uwv-ca
    image: eclipse-temurin:17-jre-jammy
    command:
      - bash
      - -c
      - |
        set -eu
        cp "$JAVA_HOME/lib/security/cacerts" /shared-truststore/cacerts
        chmod u+w /shared-truststore/cacerts
        # uwv-ca-bundle is een GECONCATENEERDE PEM (Mozilla public roots +
        # uwv-platform-CA). `keytool -importcert -alias X -file <multi-pem>`
        # importeert ALLEEN de eerste cert — alle andere worden genegeerd
        # zonder warning. Daarom split én loop, met een unieke alias per cert.
        cd /tmp
        csplit -z -f om-cert- -b "%03d.pem" /uwv-ca/ca.crt '/-----BEGIN CERTIFICATE-----/' '{*}' >/dev/null
        i=0
        for cert in om-cert-*.pem; do
          [ -s "$cert" ] || continue
          keytool -importcert -noprompt -trustcacerts \
            -keystore /shared-truststore/cacerts \
            -storepass changeit \
            -alias "uwv-ca-$i" \
            -file "$cert" >/dev/null 2>&1 || true
          i=$((i+1))
        done
        echo "Imported $i certificates into truststore"
    volumeMounts:
      - name: uwv-ca
        mountPath: /uwv-ca
        readOnly: true
      - name: shared-truststore
        mountPath: /shared-truststore

extraVolumes:
  - name: uwv-ca
    configMap:
      name: uwv-ca-bundle
  - name: shared-truststore
    emptyDir: {}

extraVolumeMounts:
  - name: shared-truststore
    mountPath: /shared-truststore
    readOnly: true

extraEnvs:
  # JAVA_TOOL_OPTIONS wordt door élk java-proces (incl. Dropwizard's
  # entrypoint /openmetadata-start.sh) automatisch toegepast — dit is
  # de standaard manier om extra JVM-args mee te geven zonder image-
  # specifieke env-vars als JAVA_OPTS / JAVA_OPTS_APPEND te gebruiken.
  #
  # `preferIPv4Stack=true` voorkomt UnknownHostException bij OIDC-discovery:
  # CoreDNS' hosts-plugin geeft AAAA-queries SERVFAIL ipv NOERROR-empty,
  # waardoor Java's getaddrinfo de IPv4-fallback nooit probeert.
  - name: JAVA_TOOL_OPTIONS
    value: >-
      -Djavax.net.ssl.trustStore=/shared-truststore/cacerts
      -Djavax.net.ssl.trustStorePassword=changeit
      -Djava.net.preferIPv4Stack=true

resources:
  requests:
    cpu: 250m
    memory: 1Gi
  limits:
    cpu: 1
    memory: 2Gi

# Startup is traag op een laptop-k3d (DB-migraties + OpenSearch-index init +
# Quartz scheduler bootstrap). De chart-default geeft 5×60s = 5 min budget.
# 30×60s = 30 min is ruim voldoende en stoort verder niks (startup probe
# stopt zodra hij ooit slaagt).
startupProbe:
  periodSeconds: 60
  failureThreshold: 30
  successThreshold: 1
  httpGet:
    path: /healthcheck
    port: http-admin

replicaCount: 1

# Chart-managed ingress on .uwv-platform.local for in-cluster routing.
# Public AKS access at openmetadata.eu-sovereigndataplatform.com is in
# infrastructure/azure/public-ingresses.yaml (`openmetadata-public`).
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: uwv-platform-issuer
  hosts:
    - host: openmetadata.uwv-platform.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - hosts:
        - openmetadata.uwv-platform.local
      secretName: openmetadata-tls

# Persistentie van logs/configs — storageClass per-mode in values-{k3d,aks}.yaml.
persistence:
  enabled: true
  size: 5Gi
